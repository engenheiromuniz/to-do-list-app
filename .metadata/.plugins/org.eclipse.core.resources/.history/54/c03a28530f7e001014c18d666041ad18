<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom styles can go here if needed */
        body {
            font-family: 'Inter', sans-serif;
            @apply bg-gray-100;
        }
        .task-card {
            @apply bg-white p-4 rounded-lg shadow-md mb-4 flex items-center justify-between;
        }
        .task-actions {
            @apply flex space-x-2;
        }
        .edit-icon, .delete-icon {
            @apply w-5 h-5; /* Adjust icon size as needed */
        }
        .completed-task .task-title {
            @apply line-through text-gray-500;
        }
    </style>
</head>
<body class="p-4 md:p-8">
    <div class="max-w-4xl mx-auto">
        <h1 class="text-4xl font-bold text-gray-800 mb-6 text-center">Your To-Do List</h1>

        <a th:href="@{/tasks/new}" class="block text-center mb-6">
            <button class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg shadow-lg transition duration-300 ease-in-out transform hover:scale-105">
                Add New Task
            </button>
        </a>

        <div th:if="${tasks.isEmpty()}" class="text-center text-gray-600 text-lg py-8">
            No tasks found. Start by adding a new one!
        </div>

        <div class="space-y-4">
            <div th:each="task : ${tasks}" class="task-card" th:classappend="${task.status == 'Completed' ? 'completed-task' : ''}">
                <div class="flex-grow">
                    <h2 class="task-title text-xl font-semibold text-gray-800" th:text="${task.title}">Task Title</h2>
                    <p class="text-gray-600 text-sm" th:text="${task.description}">Task Description</p>
                    <div class="text-gray-500 text-xs mt-1">
                        <span th:if="${task.startDate}">Start: <span th:text="${task.startDate}"></span> | </span>
                        <span th:if="${task.dueDate}">Due: <span th:text="${task.dueDate}"></span> | </span>
                        <span th:if="${task.priority}">Priority: <span th:text="${task.priority}"></span> | </span>
                        <span th:if="${task.status}">Status: <span th:text="${task.status}"></span> | </span>
                        <span th:if="${task.tags}">Tags: <span th:text="${task.tags}"></span></span>
                    </div>
                </div>
                <div class="task-actions">
                    <a th:href="@{/tasks/edit/{id}(id=${task.id})}" title="Edit Task"
                       class="text-green-500 hover:text-green-700 p-2 rounded-full hover:bg-gray-100 transition duration-150 ease-in-out">
                        <!-- Edit Icon (SVG) -->
                        <svg class="edit-icon" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                            <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zm-3.103 9.923L7.757 15.657 3 10.901l5.657-5.657 4.901-4.901 2.828 2.828-4.901 4.901z"></path>
                        </svg>
                    </a>
                    <a th:href="@{/tasks/delete/{id}(id=${task.id})}" title="Delete Task"
                       onclick="return confirm('Are you sure you want to delete this task?');"
                       class="text-red-500 hover:text-red-700 p-2 rounded-full hover:bg-gray-100 transition duration-150 ease-in-out">
                        <!-- Delete Icon (SVG) -->
                        <svg class="delete-icon" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                            <path fill-rule="evenodd" d="M9 2a1 1 0 00-.894.553L7.382 4H4a1 1 0 000 2v10a2 2 0 002 2h8a2 2 0 002-2V6a1 1 0 100-2h-3.382l-.724-1.447A1 1 0 0011 2H9zM7 8a1 1 0 011-1h4a1 1 0 110 2H8a1 1 0 01-1-1zm1 3a1 1 0 000 2h4a1 1 0 000-2H8z" clip-rule="evenodd"></path>
                        </svg>
                    </a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
